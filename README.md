# curso-node-tabla
estos son mis primeros pasos en Node

```
Options:
      --help          Show help                                        [boolean]
      --version       Show version number                              [boolean]
  -b, --numero1       es la base de la tabla de multiplicar  [number] [required]
  -l, --listar        lista la tabla de multiplicar   [boolean] [default: false]
  -f, --multiplicars  cantidad a multiplicar                 [number] [required]
```
